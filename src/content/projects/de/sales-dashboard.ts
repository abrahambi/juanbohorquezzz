import type { ProjectContent } from "../../types";
export default { title: "Sales Dashboard", theme: "dark", tags: ["powerbi", "dax", "dataviz"], description: "Dashboard integral de rendimiento de ventas." } as const satisfies ProjectContent;
