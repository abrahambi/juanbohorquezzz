import type { ProjectContent } from "../../types";

export default {
  title: "Facebook Dashboard",
  theme: "dark",
  tags: ["powerbi", "dataviz"],
  description: "Facebook social media analytics dashboard built with Power BI.",
} as const satisfies ProjectContent;
