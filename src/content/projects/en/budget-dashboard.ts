import type { ProjectContent } from "../../types";

export default {
  title: "Budget Dashboard",
  theme: "dark",
  tags: ["powerbi", "excel", "dax"],
  description: "Budget tracking and allocation analysis dashboard built with Power BI.",
} as const satisfies ProjectContent;
