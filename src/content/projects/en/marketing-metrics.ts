import type { ProjectContent } from "../../types";

export default {
  title: "Marketing Metrics Report",
  theme: "dark",
  tags: ["powerbi", "dataviz", "excel"],
  description: "Marketing campaign metrics and ROI analysis built with Power BI.",
} as const satisfies ProjectContent;
